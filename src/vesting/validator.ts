/**
 * TODO(#1154): add vesting validator-leg actions once the consensus PR lands.
 * v0.6-dev only exposes vesting beneficiary delegator actions on Vesting.sol;
 * vestingValidatorDeposit and related calls are intentionally not available yet.
 */
export interface VestingValidatorActionsPendingConsensus1154 {
  readonly consensusPr: "#1154";
}
